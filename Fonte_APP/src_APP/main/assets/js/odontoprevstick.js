// Slick //
$(document).on('ready', function() {
			
    $('.single-item').slick({
        lazyLoad: 'ondemand', // ondemand progressive anticipated
        infinite: true,
        dots: true
    });
});